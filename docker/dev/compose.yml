services:
  web:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    container_name: cashya-shoppy-web
    restart: unless-stopped
    env_file:
      - ../../.env.dev
    volumes:
      - ../../:/app
      - ../../data:/app/data
    ports:
      - 5025:8000
