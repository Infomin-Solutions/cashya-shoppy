services:
  backend:
    build:
      context: ../../
      dockerfile: docker/dev/Dockerfile
    container_name: cashya-shoppy-backend
    restart: unless-stopped
    env_file:
      - ../../.env
    volumes:
      - ../../:/app
      - ../../data:/app/data
    ports:
      - 8000:8000
